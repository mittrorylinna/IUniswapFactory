{
  "name": "apollumia-token",
  "version": "1.0.0",
  "description": "APOLLUMIA ERC-20 Token Project",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network rinkeby"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.0",
    "hardhat": "^2.17.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  },
  "keywords": ["solidity", "erc20", "hardhat", "ethereum"],
  "license": "MIT"
}
